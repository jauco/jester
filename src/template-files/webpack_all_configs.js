module.exports.entryFilesSettings = {
    //enter your own settings here.
    //these settings will only apply when generating the entrypoint artifacts
};
module.exports.testFilesSettings = {
    //enter your own settings here.
    //these settings will only apply when generating an artifact for testing in karma
};
module.exports.sharedSettings = {
    //enter your own settings here.
    //these settings will be shared and overridden by the above settings
};